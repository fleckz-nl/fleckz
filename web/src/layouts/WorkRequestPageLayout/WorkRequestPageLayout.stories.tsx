import type { Meta, StoryObj } from '@storybook/react'

import WorkRequestPageLayout from './WorkRequestPageLayout'

const meta: Meta<typeof WorkRequestPageLayout> = {
  component: WorkRequestPageLayout,
}

export default meta

type Story = StoryObj<typeof WorkRequestPageLayout>

export const Primary: Story = {}
