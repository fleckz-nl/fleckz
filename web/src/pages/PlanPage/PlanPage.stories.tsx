import type { Meta, StoryObj } from '@storybook/react'

import PlanPage from './PlanPage'

const meta: Meta<typeof PlanPage> = {
  component: PlanPage,
}

export default meta

type Story = StoryObj<typeof PlanPage>

export const Primary: Story = {}
